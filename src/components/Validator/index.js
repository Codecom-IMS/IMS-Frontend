const studentFieldsValidator = (
  studentName,
  fatherName,
  fatherCNIC,
  basicFee,
  others,
  dateOfBirth,
  className
) => {
  if (!studentName) {
  } else if (!fatherName) {
  } else if (!fatherCNIC) {
  } else if (!basicFee) {
  } else if (!others) {
  } else if (!dateOfBirth) {
  } else if (!className) {
  } else {
  }
};
