import React from "react";
import { ToastContainer } from "react-toastify";

const Toast = () => {
  return <ToastContainer position="top-right" autoClose={2000} theme="light" />;
};

export default Toast;
